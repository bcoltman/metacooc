# API reference

The high-level Python pipelines mirror the CLI analyses. Their docstrings describe the current function signatures and return values. For a stable, reproducible interface, prefer the CLI and pass an explicit data release or custom path when calling Python functions.

The command line and analysis-specific output pages are the source of truth for option names, output filenames, summary columns, and plotting behaviour.
