# Command-line reference

The following pages are generated directly from MetaCoOc’s argument parser, so option names and defaults stay aligned with the installed package:

```{eval-rst}
.. argparse::
   :module: metacooc.cli
   :func: build_parser
   :prog: metacooc
```

Use the [recommended workflow](workflow.md) for guidance on which options to choose; this page is a reference rather than a tutorial.
